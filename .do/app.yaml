name: portfolio
region: nyc
static_sites:
  - name: portfolio-static
    github:
      repo: TorokDan/portfolio
      branch: master
      deploy_on_push: false
    build_command: npm ci && npm run build
    output_dir: build
    routes:
      - path: /
